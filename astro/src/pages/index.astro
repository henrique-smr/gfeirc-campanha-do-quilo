---
import '../styles/global.css';
import { Icon } from 'astro-icon/components'
import {Lista} from '@/components/Lista.tsx';
import ModalDoacao from '@/components/ModalDoacao.astro';
import ModalPix from '@/components/ModalPix.astro';
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Campanha do Quilo - GFEIRC</title>
	</head>
	<body class="text-base-content bg-base-100 text-justify">
		<div class="sm:mx-10 pt-10  p-4 flex flex-col items-center justify-start min-h-screen prose prose-p:my-3 max-w-none">
			<div class="grid grid-cols-1 lg:grid-cols-2 gap-x-10 gap-y-4 max-w-5xl">
				<div class="flex justify-center gap-4 items-center">
					<div class="mb-3">
						<Icon width={72} height={72}
							name="potoffood"/>
					</div>
					<h1 class="text-amber-900 m-0!">
						Campanha do Quilo 
					</h1>
				</div>
				<div></div>
				<div class="flex flex-col gap-4">
					<div class="card rounded-none bg-base-200 card-border border-base-300 p-4">
						<p>
							A <b class="font-semibold">Campanha do Quilo</b> é uma ação fraterna promovida por nossa Casa Espírita, com o objetivo de arrecadar alimentos não perecíveis para auxiliar famílias em situação de vulnerabilidade social. Atualmente, mais de 130 famílias cadastradas recebem mensalmente esse apoio.
						</p>
						<p>
							Para contribuir, você pode doar alimentos não perecíveis, usando essa nossa <b class="font-semibold">cesta básica online</b>. 
						</p>
						<p>
							Basta escolher os itens que deseja doar, e vamos gerar um Pix para facilitar a sua contribuição.
						</p>
					</div>
				</div>
				<div class="flex flex-col gap-4 ">
					<Lista client:load/>
				</div>
			</div>
		</div>

		<footer>
			<div class="text-center text-sm text-base-content/50">
				<h3 class="">Grupo Fraternidade Espírita Irmão Rogério Cláudio</h4>
			</div>

		</footer>
		<ModalDoacao/>
		<ModalPix/>
	</body>
	<script>
	import { auth } from '../lib/firebase/client';
		import { signInAnonymously, signOut } from 'firebase/auth'

		console.log('Firebase Auth initialized:', auth);

		signInAnonymously(auth)
			.then(() => {
				console.log('Signed in anonymously');
			})
			.catch((error) => {
				console.error('Error signing in anonymously:', error);
			});
	</script>
</html>
